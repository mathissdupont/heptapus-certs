# LMS CLI archive

The former `hc lms` command group is retained here for reference. It is outside
the `heptacert_cli` package and is intentionally not registered by the active
CLI because its backend routes are archived.
